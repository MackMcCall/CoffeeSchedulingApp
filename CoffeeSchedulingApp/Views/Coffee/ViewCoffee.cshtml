@using CoffeeSchedulingApp.Models.CoffeeModelsRepos;
@model Coffee

<h3>@Model.Roaster @Model.Producer Information</h3>
<table class="table">
    <tr>
        <th>CoffeeID</th>
        <th>Roaster</th>
        <th>Producer</th>
        <th>Country</th>
        <th>Region</th>
        <th>Variety</th>
        <th>Process</th>
        <th>Roast Date</th>
        <th>Days Rest Needed</th>
        <th>Ready To Drink</th>
        <th>Grams</th>
    </tr>

    <tr>
        <td><a href=/Coffee/ViewCoffee/@Model.CoffeeID>@Model.CoffeeID</a></td>
        <td>@Model.Roaster</td>
        <td>@Model.Producer</td>
        <td>@Model.Country</td>
        <td>@Model.Region</td>
        <td>@Model.Variety</td>
        <td>@Model.Process</td>
        <td>@Model.RoastDate.Date.Month/@Model.RoastDate.Date.Day</td>
        <td>@Model.DaysRestNeeded</td>
        <td>@(@Model.ReadyToDrink ? "Yes" : "No")</td>
        <td>@Model.Grams</td>
    </tr>
    
</table>

<div style="display: block">
    <a href="/Coffee/UpdateCoffee/@Model.CoffeeID">Update this Coffee</a>
</div>

<div style="display: block">

    @using (Html.BeginForm("UpdateCoffee", "Coffee", "Post"))
    {
        <input type="hidden" asp-for="CoffeeID" value="@Model.CoffeeID" />
        <input type="submit" value="Update this Coffee" />
    }
</div>

<div style="display: block">

    @using (Html.BeginForm("DeleteCoffee", "Coffee", "Post"))
    {
        <input type="hidden" asp-for="CoffeeID" value="@Model.CoffeeID" />
        <input type="submit" onclick="return confirm('Are you sure?')" value="Delete this Coffee" />
    }
</div>